name: Matrix
on: [push]

jobs:
    node-matrix:
        runs-on: ubuntu-latest
        steps:
            - run: node -v
            - uses: actions/setup-node@v4
              with:
                  node-version: 14
            - run: node -v
